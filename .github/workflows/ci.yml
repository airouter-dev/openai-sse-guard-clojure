name: Clojure CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:
    inputs:
      checkout-ref:
        description: 'Optional commit or tag to validate'
        required: false
        type: string

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: ['11', '17', '21']
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.checkout-ref || github.sha }}
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - name: Install Leiningen
        run: curl -fsSL https://raw.githubusercontent.com/technomancy/leiningen/stable/bin/lein > "$RUNNER_TEMP/lein" && chmod +x "$RUNNER_TEMP/lein" && echo "$RUNNER_TEMP" >> "$GITHUB_PATH"
      - run: lein test
      - run: lein pom
